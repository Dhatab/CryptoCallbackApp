<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cryptocallback.cryptocallback">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".Graphs.Graph_AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_ccb_square"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_ccb_circle"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogIn.SignIn">
        </activity>
        <activity android:name=".LogIn.Registration" />
        <activity android:name=".LogIn.PasswordForgot" />
        <activity android:name=".CoinDetailActivity.CoinDetailActivity" />
        <activity android:name=".MainHomeActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".AboutUsActviity" />
        <activity android:name=".SplashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>